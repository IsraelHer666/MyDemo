git init crea una carpeta con repositorio 

git add añade archivos al area de staging para
saber si estos archivos son correctos para 
empezar a commitear

git diff sirve para ver las diferencias de un archivo con 
control de versiones.

git diff (nombre del archivo)

git checkout permite retroceder una version del archivo comiteado

git remote add (name) le dice al repositorio local que se creo uno remoto
se le da un nombre, casi siempre deberia de ser origin y el link del repo

git push -u origin main unimos el repositorio local con el principal (main) por defecto.
